@model  List<ChannakyaAccounting.Models.Models.FinSys2>

<div class="container">
    <div class="row">
        <div class="col-md-12">
            <div class="table-responsive">

                <table id="mytable" class="table table-bordred table-striped" style="max-width:75%;">

                    <thead>
                        
                            <th>
                                Node Name
                            </th>
                            <th>
                                IsGroup
                            </th>
                            <th>
                                IsContext
                            </th>
                        
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            @Html.Partial("_ListView", item)
                        }

                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

