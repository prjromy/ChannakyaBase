@model ChannakyaAccounting.Models.ViewModel.usertovoucherreturnViewModel
    @using ChannakyaAccounting.Service.Voucher1
@{
    var test = Model;
    Voucher1Service getname = new Voucher1Service();
    
}

@foreach (var item in Model.voucherreturnViewModel)
{
    
    var username =getname.GetUserNameforDescription(item.userid);
    @*@Html.HiddenFor(Model=>Model.FYID,new { @id= "FYID" })*@
<tr class="detailsList" style="display:table-row !important;">
    <td class="listDrill" id="@item.userid" style="vertical-align:top;">
        @*<a class="inner listDrill" uservsvoucher-caption="@username" style="cursor:pointer;">@username</a>*@
        @username
    </td>
    @*<td>
        @getname.GetUserNameforDescription( item.userid)
    </td>*@
    <td>           
      @getname.GetVoucherTypeNameforDescription(item.Vtype)
    </td>
    <td>
        @*@getname.GetBatchNamefordescription(item.Batchtype)*@
        @item.BatchList
    </td>

    <td>
        @*<input type="button" id="@Model.MenuId" class="btn btn-link editList" value="Edit" />
            <input type="button" id="@Model.MenuId" class="btn btn-link deleteNode" value="Delete" />*@
    <td><p data-placement="top" data-toggle="tooltip" title="Edit"><button class="btn btn-primary btn-xs editList" id="@item.userid" Vtype="@item.Vtype" data-title="Edit"><span class="glyphicon glyphicon-pencil"></span></button></p></td>
    <td><p data-placement="top" data-toggle="tooltip" title="Delete"><button class="btn btn-danger btn-xs deleteNode" id="@item.userid" Vtype="@item.Vtype" data-title="Delete"  ><span class="glyphicon glyphicon-trash"></span></button></p></td>
</tr>
}
