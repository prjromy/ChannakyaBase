@model ChannakyaBase.Model.ViewModel.DisburseShareViewModel

<fieldset>
    <legend>Disburse Share</legend>

    <div class="table table-responsive table-stripped table-bordered">
        <table class="table table-responsive table-hover cust-contact-header-table">
            <thead>
                <tr>
                    <th class="col-md-3">Customer Name</th>
                    <th class="col-md-3">Rate</th>
                    <th class="col-md-3">Quantity</th>
                    <th class="col-md-3">Amount</th>
                </tr>
            </thead>


            @*@for (int i = 0; i < Model.DisburseShareList.Count; i++)
            {
                <tr>
                    @Html.HiddenFor(model => Model.DisburseShareList[i].RegNo)
                    <td>
                        @Html.DisplayFor(model => Model.DisburseShareList[i].Name, new { htmlAttributes = new { @class = "form-control" } })
                    </td>

                    <td>
                        @Html.EditorFor(model => Model.DisburseShareList[i].Rate, new { htmlAttributes = new { @class = "form-control share-rate", @readonly = "readonly" } })
                    </td>
                    <td>
                        @Html.EditorFor(model => Model.DisburseShareList[i].SQty, new { htmlAttributes = new { @class = "form-control share-quantity", @id = "SQty" } })
                    </td>
                    <td>
                        @Html.EditorFor(model => Model.DisburseShareList[i].Amount, new { htmlAttributes = new { @class = "form-control share-amount", @readonly = "readonly" } })
                    </td>

                </tr>

            }*@
            @foreach (var item in Model.DisburseShareList)
            {
                Html.RenderPartial("_DisbursePartial", item);
            }
        </table>

    </div>
</fieldset>

<script>
    
</script>