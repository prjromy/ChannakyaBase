@model  List<ChannakyaAccounting.Models.Models.VoucherType>

<div class="">
    <div class="row">
        <div class="col-md-12">
            <div class="table-responsive">

                <table id="mytable" class="table table-bordred table-striped" style="max-width:100%;">

                    <thead>

                    <th>
                        Voucher Name
                    </th>
                    <th>
                      Prefix
                    </th>
                

                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            @Html.Partial("_ListView", item)
                        }

                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>


