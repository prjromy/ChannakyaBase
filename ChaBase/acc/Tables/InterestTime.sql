CREATE TABLE [acc].[InterestTime] (
    [Id]           INT          IDENTITY (1, 1) NOT NULL,
    [InterestTime] VARCHAR (50) NULL,
    CONSTRAINT [PK_InterestTime] PRIMARY KEY CLUSTERED ([Id] ASC)
);

